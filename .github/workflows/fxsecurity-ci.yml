name: "fxsecurity-ci"

on:
  workflow_dispatch:
    inputs:
      module:
        description: "Go module path to test"
        required: false
        type: string
        default: "pkg/fx/fxsecurity"
  push:
    branches:
      - "feat**"
      - "fix**"
      - "hotfix**"
      - "chore**"
      - "main"
    paths:
      - "pkg/fx/fxsecurity/**.go"
      - "pkg/fx/fxsecurity/go.mod"
      - "pkg/fx/fxsecurity/go.sum"
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
    branches:
      - main
    paths:
      - "pkg/fx/fxsecurity/**.go"
      - "pkg/fx/fxsecurity/go.mod"
      - "pkg/fx/fxsecurity/go.sum"

jobs:
  ci:
    uses: ./.github/workflows/common-ci.yml
    secrets: inherit
    with:
      module: "pkg/fx/fxsecurity"
