name: "fxblob-ci"

on:
  workflow_dispatch:
    inputs:
      module:
        description: "Go module path to test"
        required: false
        type: string
        default: "pkg/fx/fxblob"
  push:
    branches:
      - "feat**"
      - "fix**"
      - "hotfix**"
      - "chore**"
      - "main"
    paths:
      - "pkg/fx/fxblob/**.go"
      - "pkg/fx/fxblob/go.mod"
      - "pkg/fx/fxblob/go.sum"
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
    branches:
      - main
    paths:
      - "pkg/fx/fxblob/**.go"
      - "pkg/fx/fxblob/go.mod"
      - "pkg/fx/fxblob/go.sum"

jobs:
  ci:
    uses: ./.github/workflows/common-ci.yml
    secrets: inherit
    with:
      module: "pkg/fx/fxblob"
