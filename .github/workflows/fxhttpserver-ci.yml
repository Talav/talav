name: "fxhttpserver-ci"

on:
  workflow_dispatch:
    inputs:
      module:
        description: "Go module path to test"
        required: false
        type: string
        default: "pkg/fx/fxhttpserver"
  push:
    branches:
      - "feat**"
      - "fix**"
      - "hotfix**"
      - "chore**"
      - "main"
    paths:
      - "pkg/fx/fxhttpserver/**.go"
      - "pkg/fx/fxhttpserver/go.mod"
      - "pkg/fx/fxhttpserver/go.sum"
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
    branches:
      - main
    paths:
      - "pkg/fx/fxhttpserver/**.go"
      - "pkg/fx/fxhttpserver/go.mod"
      - "pkg/fx/fxhttpserver/go.sum"

jobs:
  ci:
    uses: ./.github/workflows/common-ci.yml
    secrets: inherit
    with:
      module: "pkg/fx/fxhttpserver"
