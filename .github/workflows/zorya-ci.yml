name: "zorya-ci"

on:
  workflow_dispatch:
    inputs:
      module:
        description: "Go module path to test"
        required: false
        type: string
        default: "pkg/module/zorya"
  push:
    branches:
      - "feat**"
      - "fix**"
      - "hotfix**"
      - "chore**"
      - "main"
    paths:
      - "pkg/module/zorya/**.go"
      - "pkg/module/zorya/go.mod"
      - "pkg/module/zorya/go.sum"
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
    branches:
      - main
    paths:
      - "pkg/module/zorya/**.go"
      - "pkg/module/zorya/go.mod"
      - "pkg/module/zorya/go.sum"

jobs:
  ci:
    uses: ./.github/workflows/common-ci.yml
    secrets: inherit
    with:
      module: "pkg/module/zorya"
