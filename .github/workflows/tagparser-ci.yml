name: "tagparser-ci"

on:
  workflow_dispatch:
    inputs:
      module:
        description: "Go module path to test"
        required: false
        type: string
        default: "pkg/component/tagparser"
  push:
    branches:
      - "feat**"
      - "fix**"
      - "hotfix**"
      - "chore**"
      - "main"
    paths:
      - "pkg/component/tagparser/**.go"
      - "pkg/component/tagparser/go.mod"
      - "pkg/component/tagparser/go.sum"
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
    branches:
      - main
    paths:
      - "pkg/component/tagparser/**.go"
      - "pkg/component/tagparser/go.mod"
      - "pkg/component/tagparser/go.sum"

jobs:
  ci:
    uses: ./.github/workflows/common-ci.yml
    secrets: inherit
    with:
      module: "pkg/component/tagparser"
