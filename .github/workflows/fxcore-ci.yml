name: "fxcore-ci"

on:
  workflow_dispatch:
    inputs:
      module:
        description: "Go module path to test"
        required: false
        type: string
        default: "pkg/fx/fxcore"
  push:
    branches:
      - "feat**"
      - "fix**"
      - "hotfix**"
      - "chore**"
      - "main"
    paths:
      - "pkg/fx/fxcore/**.go"
      - "pkg/fx/fxcore/go.mod"
      - "pkg/fx/fxcore/go.sum"
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
    branches:
      - main
    paths:
      - "pkg/fx/fxcore/**.go"
      - "pkg/fx/fxcore/go.mod"
      - "pkg/fx/fxcore/go.sum"

jobs:
  ci:
    uses: ./.github/workflows/common-ci.yml
    secrets: inherit
    with:
      module: "pkg/fx/fxcore"
